// require("./xyz.js");
// const { x, calculateSum } = require("./sum.js");

const { calculateSum, substraction } = require("./calculate");

// import { z, multiply } from "./dev.js";
let a = 10;
let b = 20;

calculateSum(a, b);
substraction(a, b);

calculateSum(a, b);
// multiply(a, b);
